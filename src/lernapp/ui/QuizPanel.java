package lernapp.ui;

import javax.swing.JLabel;
import javax.swing.JPanel;

// Platzhalter fuer den Quiz-Modus. Wird in einem spaeteren Schritt
// mit echten Fragen und Buttons gefuellt.
public class QuizPanel extends JPanel {

    public QuizPanel() {
        add(new JLabel("Quiz-Modus (kommt noch)"));
    }
}
